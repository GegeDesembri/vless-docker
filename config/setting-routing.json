{
  "routing": {
    "rules": [
      {
        "type": "field",
        "port": "53",
        "outboundTag": "direct"
      },
      {
        "inboundTag": [
        "api"
      ],
        "outboundTag": "api",
        "type": "field"
      }
    ],
    "domainStrategy": "IPOnDemand",
    "domainMatcher": "linear"
  }
}
